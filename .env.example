# No runtime environment variables are required for local development.
#
# GitHub release signing uses repository secrets instead of .env values:
# - TAURI_SIGNING_PRIVATE_KEY
# - TAURI_SIGNING_PRIVATE_KEY_PASSWORD
